name: dinero
description: "Exchange - \"Dinero\"."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6

  # State management & Architecture.
  flutter_bloc: ^8.1.6

  # Components for Network Layer.
  dio: ^5.5.0+1
  retrofit: ^4.1.0
  pretty_dio_logger: ^1.3.1
  json_annotation: ^4.9.0

  # Service Locator for Dart and Flutter.
  get_it: ^7.7.0
  injectable: ^2.4.2

  # Code generator for data classes, unions, and cloning.
  freezed: ^2.5.2
  freezed_annotation: ^2.4.3

  # A declarative routing package for Flutter.
  go_router: ^14.2.1

  # Storage for simple data (Key - Value).
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.2.3

  # Draw SVG files using Flutter.
  flutter_svg: ^2.0.10+1

  # Easy translations for many languages.
  easy_localization: ^3.0.7

  # More customization when notifying your user.
  another_flushbar: ^1.12.30

  # A flutter plugin for adapting screen and font size.
  flutter_screenutil: ^5.9.3

  # A mapper that maps between different objects with ease.
  auto_mappr: ^2.5.0

  # Provides internationalization and localization facilities, including message translation,
  # plurals and genders, date/number formatting and parsing, and bidirectional text.
  intl: ^0.19.0

  # FL Chart is a highly customizable Flutter chart library.
  fl_chart: ^0.68.0

  # The Flutter Charts package is a data visualization library written natively in Dart
  # for creating beautiful, animated and high-performance charts.
  syncfusion_flutter_charts: ^26.2.8

  # Package to help you lazily load and display small chunks of items
  # as the user scrolls down the screen – known as infinite scrolling pagination.
  infinite_scroll_pagination: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^3.0.0

  # The build_runner package provides a concrete way of generating files using Dart code.
  build_runner: ^2.4.11

  # A generator for Injectable library.
  injectable_generator: ^2.6.1

  # A generator for Retrofit library.
  retrofit_generator: ^8.1.1

  # A generator for GoRouter library.
  go_router_builder: ^2.7.0

  # Provides Dart Build System builders for handling JSON.
  json_serializable: ^6.8.0

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/translations/
    - assets/images/

  fonts:
    - family: Urbanist
      fonts:
        - asset: assets/fonts/urbanist_regular.ttf
          weight: 400
        - asset: assets/fonts/urbanist_medium.ttf
          weight: 500
        - asset: assets/fonts/urbanist_semi_bold.ttf
          weight: 600
        - asset: assets/fonts/urbanist_bold.ttf
          weight: 700
